// export const auttJWT = async (req, res, next) => {
//   try {
//     const authorization =
//       (req.cookies && req.cookies["Authorization"]) ||
//       (req.header("Authorization") ? req.header("Authorization") : null);

//   } catch (error) {}
// };
